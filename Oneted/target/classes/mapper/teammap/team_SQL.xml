<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team">

	<select id="searchTeamList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    team_board
			WHERE
				lower(tname) like '%'||#{search}||'%'
			ORDER BY
				tseq desc
		]]>
	</select>
	
	<select id="searchProjectList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    project_board
			WHERE
				lower(pname) like '%'||#{search}||'%'
			ORDER BY
				pseq desc
		]]>
	</select>
	
	<select id="selectTeamList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    team_board
			ORDER BY
				tseq desc
		]]>
	</select>
	
	<select id="selectProjectList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    project_board
			ORDER BY
				pseq desc
		]]>
	</select>
	
	<select id="selectTeamOne" parameterType="int" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    team_board
			WHERE
				TSEQ = #{IDX}
		]]>
	</select>
	
	<select id="selectProjectOne" parameterType="int" resultType="hashmap">
		<![CDATA[
			SELECT
				*
			FROM
			    project_board
			WHERE
				PSEQ = #{IDX2}
		]]>
	</select>
	
</mapper>